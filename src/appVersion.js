/**
 * Shown in the feature nav footer. When committing after a request that includes a version
 * bump, increment by 1 (this commit stays at 25; next bump → 26).
 */
export const APP_DISPLAY_VERSION = 25
